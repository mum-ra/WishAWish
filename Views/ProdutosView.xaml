<UserControl x:Class="WishAWish.Views.ProdutosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WishAWish.ViewModels">
    <UserControl.DataContext>
        <vm:ProdutosViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0 0 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Grid.Column="0">
                <WrapPanel>
                    <TextBlock Text="Nome:" VerticalAlignment="Center"/>
                    <TextBox Width="200" Margin="6,0,20,0"
                             Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Código:" VerticalAlignment="Center"/>
                    <TextBox Width="150" Margin="6,0,20,0"
                             Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Valor Mín:" VerticalAlignment="Center"/>
                    <TextBox Width="90" Margin="6,0,6,0"
                             Text="{Binding ValorMin, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Valor Máx:" VerticalAlignment="Center"/>
                    <TextBox Width="90" Margin="6,0,0,0"
                             Text="{Binding ValorMax, UpdateSourceTrigger=PropertyChanged}"/>
                </WrapPanel>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Column="1" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource ToolbarButton}" Content="Novo"   Command="{Binding NovoCmd}"/>
                    <Button Style="{StaticResource ToolbarButton}" Content="Salvar" Command="{Binding SalvarCmd}"/>
                    <Button Content="Excluir" Command="{Binding ExcluirCmd}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Nome:" Margin="0 0 6 6"/>
                <TextBox   Grid.Row="0" Grid.Column="1" Margin="0 0 0 6"
                           Text="{Binding Selecionado.Nome, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Text="Código:" Margin="0 0 6 6"/>
                <TextBox   Grid.Row="1" Grid.Column="1" Margin="0 0 0 6"
                           Text="{Binding Selecionado.Codigo, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Text="Valor:" Margin="0 0 6 0"/>
                <TextBox   Grid.Row="2" Grid.Column="1"
                           Text="{Binding Selecionado.Valor, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding ProdutosView}"
                      SelectedItem="{Binding Selecionado}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="150"/>
                    <DataGridTextColumn Header="Valor"
                                        Binding="{Binding Valor, StringFormat=\{0:C2\}}"
                                        Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
